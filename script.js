const navLinks=[...document.querySelectorAll("nav a")];const sections=navLinks.map(l=>document.querySelector(l.getAttribute("href"))).filter(Boolean);const header=document.querySelector("header");const nav=document.querySelector("nav");let activeId="accueil";let ticking=false;function moveIndicator(link){if(!nav||!link)return;const n=nav.getBoundingClientRect(),r=link.getBoundingClientRect();nav.style.setProperty("--indicator-left",r.left-n.left+"px");nav.style.setProperty("--indicator-width",r.width+"px");nav.classList.add("ready")}function setActive(id){const link=navLinks.find(l=>l.getAttribute("href")==="#"+id)||navLinks[0];if(!link)return;activeId=id;navLinks.forEach(l=>l.classList.toggle("active",l===link));moveIndicator(link)}function updateHeader(){header?.classList.toggle("scrolled",window.scrollY>20)}const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(visible&&visible.target.id!==activeId)setActive(visible.target.id)},{rootMargin:"-18% 0px -62% 0px",threshold:[0,.15,.35,.6]});sections.forEach(s=>observer.observe(s));function revealSetup(){const targets=document.querySelectorAll(".section,.project,.skills article,.about>div,.community-card,.contact,.section-head");targets.forEach((el,i)=>{el.classList.add("reveal");if(i%4===1)el.classList.add("reveal-delay-1");if(i%4===2)el.classList.add("reveal-delay-2");if(i%4===3)el.classList.add("reveal-delay-3")});const revealObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");revealObserver.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(e=>revealObserver.observe(e))}function setupCursor(){if(matchMedia("(pointer:coarse)").matches||matchMedia("(prefers-reduced-motion:reduce)").matches)return;const dot=document.createElement("div"),ring=document.createElement("div");dot.className="cursor-dot";ring.className="cursor-ring";document.body.append(dot,ring);let x=innerWidth/2,y=innerHeight/2,rx=x,ry=y;addEventListener("pointermove",e=>{x=e.clientX;y=e.clientY;document.documentElement.style.setProperty("--mx",x+"px");document.documentElement.style.setProperty("--my",y+"px");dot.style.left=x+"px";dot.style.top=y+"px"});function loop(){rx+=(x-rx)*.16;ry+=(y-ry)*.16;ring.style.left=rx+"px";ring.style.top=ry+"px";requestAnimationFrame(loop)}loop();document.querySelectorAll("a,button,.project,.skills article,.community-card").forEach(el=>{el.addEventListener("mouseenter",()=>ring.classList.add("big"));el.addEventListener("mouseleave",()=>ring.classList.remove("big"))})}function setupTilt(){if(matchMedia("(pointer:coarse)").matches)return;document.querySelectorAll(".project,.community-card").forEach(card=>{card.addEventListener("pointermove",e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform="perspective(700px) rotateX("+(-y*3)+"deg) rotateY("+(x*3)+"deg) translateY(-7px)"});card.addEventListener("pointerleave",()=>card.style.transform="")})}function setupParticles(){const hero=document.querySelector(".hero");if(!hero||matchMedia("(prefers-reduced-motion:reduce)").matches)return;const layer=document.createElement("div");layer.className="particles";layer.setAttribute("aria-hidden","true");hero.appendChild(layer);for(let i=0;i<18;i++){const p=document.createElement("i");p.style.left=Math.random()*100+"%";p.style.top=15+Math.random()*75+"%";p.style.animationDelay=Math.random()*5+"s";p.style.animationDuration=4+Math.random()*6+"s";layer.appendChild(p)}}function smoothAnchors(){document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener("click",e=>{const target=document.querySelector(link.getAttribute("href"));if(target){e.preventDefault();target.scrollIntoView({behavior:"smooth",block:"start"});setActive(target.id)}}))}addEventListener("scroll",()=>{if(!ticking){requestAnimationFrame(()=>{updateHeader();ticking=false});ticking=true}},{passive:true});addEventListener("resize",()=>moveIndicator(navLinks.find(l=>l.classList.contains("active"))||navLinks[0]));updateHeader();setActive("accueil");revealSetup();setupCursor();setupTilt();setupParticles();smoothAnchors();